# catalog

The Mine in Abyss Gradle version catalog.

## Updating

- `gradle versionCatalogUpdate` To automatically get latest versions and update catalog
- Check `gradle/libs.versions.toml` to manually update pinned versions